# android-tracker

React Native Android client for WR any% route tracking.

Sends location events to `tracking-gateway` via WebSocket.
Buffers events locally when offline and replays on reconnect.

> Implementation starts in EPIC 3: Android Background Location Tracking.
